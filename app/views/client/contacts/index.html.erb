<% @contacts.each do |contact| %>
  <h2>
    <a href= "/client/contacts/<%= contact["id"] %>"> 
      <%= contact["full_name"] %></a></h2>
  <h2>Phone Number: <%= contact["phone_numner"] %> </h2>
  <h2>Email: <%= contact["email"] %> </h2>
  <h2>Bio: <%= contact["bio"] %> </h2>
  <br>
<% end %>